workflows:
  android-workflow:
    name: Android Build
    instance_type: mac_mini_m1
    max_build_duration: 60
    environment:
      groups:
        - default
    scripts:
      - name: Build Android
        script: |
          flutter build apk --release
    artifacts:
      - build/**/outputs/**/*.apk
